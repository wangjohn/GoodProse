# Blog posts

`posts.jsonl` is created by `goodprose import-posts`. Each target remains the exact Markdown body
from the owner's export; front matter is preserved as structured metadata rather than training
text.
