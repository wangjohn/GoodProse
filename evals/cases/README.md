# Runnable eval cases

Store public development cases under `dev/` and protected source-family cases under `test/`. Each JSON or JSONL record should conform to [`../schemas/eval-case.schema.json`](../schemas/eval-case.schema.json). Keep final, non-public cases in `../private/` or external access-controlled storage.
